[
  {
    "name": "/schedule shows match schedule",
    "steps": [
      {
        "send": { "text": "/schedule" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": { "text": "No matches scheduled." }
          }
        ]
      }
    ]
  }
]
