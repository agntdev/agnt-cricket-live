[
  {
    "name": "/recent shows recently completed matches",
    "steps": [
      {
        "send": { "text": "/recent" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": { "text": "No recent matches found. Check back later!" }
          }
        ]
      }
    ]
  }
]
