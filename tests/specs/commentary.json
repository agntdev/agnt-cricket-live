[
  {
    "name": "/commentary prompts for match ID",
    "steps": [
      {
        "send": { "text": "/commentary" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": { "text": "Which match commentary would you like? Send /commentary followed by a match ID." }
          }
        ]
      }
    ]
  }
]
