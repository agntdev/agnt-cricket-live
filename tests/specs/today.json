[
  {
    "name": "/today shows today's matches",
    "steps": [
      {
        "send": { "text": "/today" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": { "text": "No matches scheduled for today. Check back later!" }
          }
        ]
      }
    ]
  }
]
