[
  {
    "name": "/scorecard prompts for match ID",
    "steps": [
      {
        "send": { "text": "/scorecard" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": { "text": "Which match scorecard would you like? Send /scorecard followed by a match ID." }
          }
        ]
      }
    ]
  }
]
