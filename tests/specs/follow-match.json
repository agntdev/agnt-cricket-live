[
  {
    "name": "Follow button shows empty state",
    "steps": [
      {
        "send": { "callback": "follow:match" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": { "text": "You're not following any matches yet. Open a live match and tap Follow to get started." }
          }
        ]
      }
    ]
  }
]
